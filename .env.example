PORT=4000
MONGODB=your_mongodb_connection_string
JWT_SECRET=your_jwt_secret

# Password reset: base URL of the React app (used in reset link emails)
FRONTEND_BASE_URL=http://localhost:3000

# Optional: CORS origin for API (defaults to http://localhost:3000)
# FRONTEND_ORIGIN=http://localhost:3000

# Optional: SMTP for sending reset emails. If not set, reset link is logged to console.
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password
# SMTP_FROM=noreply@example.com
